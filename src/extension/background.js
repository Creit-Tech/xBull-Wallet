console.log('soy el background');
// chrome.runtime.onMessageExternal.addListener((message, sender, sendResponse) => {
//   console.log({ message });
//   sendResponse(message);
// });
import { XBULL_CONNECT } from './constants';
const a = XBULL_CONNECT;
console.log(a);
