import { PressButtonDirective } from './press-button.directive';

describe('PressButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new PressButtonDirective();
    expect(directive).toBeTruthy();
  });
});
