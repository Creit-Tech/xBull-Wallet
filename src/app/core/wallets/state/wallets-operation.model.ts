import { ID } from '@datorama/akita';

export interface WalletsOperation {
  id: ID;
}

export function createWalletsOperation(params: Partial<WalletsOperation>) {
  return {

  } as WalletsOperation;
}
